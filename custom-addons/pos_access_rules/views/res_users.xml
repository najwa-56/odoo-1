<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="rules_inherit_view_users_form" model="ir.ui.view">
        <field name="name">rules.inherit.view.users.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form" />
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Point of Sale">
                    <group>
                        <field name="pos_config_id" />
                    </group>
                </page>
            </notebook>
        </field>
    </record>
       <record id="pos_config_user_restriction" model="ir.rule">
        <field name="name">RestrictPos Configs to Assigned</field>
        <field name="model_id" ref="point_of_sale.model_pos_config"/>
        <field name="domain_force">[('id' , '=',user.pos_config_id.id)] </field>
                  <field name="groups" eval="[(4, ref('point_of_sale.group_pos_user'))]" />
    </record>
    <record id="pos_config_manager_full_access" model="ir.rule">
        <field name="name">All pos manager full access to configs</field>
        <field name="model_id" ref="point_of_sale.model_pos_config"/>
        <field name="domain_force">[(1 , '=',1)] </field>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_manager'))]" />
    </record>

</odoo>
