<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- هذا xml الخاص بصفحة الزيارات -->
        <!-- Tree View for Daily Visits -->
        <record id="view_daily_visit_tree" model="ir.ui.view">
            <field name="name">daily.visit.tree</field>
            <field name="model">daily.visit</field>
            <field name="arch" type="xml">
                <tree string="الزيارات">
                    <field name="reference"/>
                    <field name="created_by"/>
                    <field name="creation_date" invisible="1"/>
                    <field name="start_time"/>
                    <field name="end_time"/>
                    <field name="partner_id"/>
                </tree>
            </field>
        </record>

        <!-- step1 create a new window action with a new domain -->
        <record id="new_act_res_partner_2_sale_order3" model="ir.actions.act_window">
            <field name="name">مبيعات اليوم</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <!--<field name="domain">[('date_order', '&gt;=', (context_today().strftime('%Y-%m-%d') + ' 00:00:00')),
                ('date_order', '&lt;=', (context_today().strftime('%Y-%m-%d') + ' 23:59:59'))
                ]
            </field>-->
        </record>

        <!-- Form View for Daily Visits -->
        <record id="view_daily_visit_form" model="ir.ui.view">
            <field name="name">daily.visit.form</field>
            <field name="model">daily.visit</field>
            <field name="arch" type="xml">
                <form string="الزيارات">
                    <sheet>
                        <div class="oe_title"> <!-- to make reference field in the header -->
                            <h1>
                                <field name="reference" readonly="1"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="reference" invisible="1"/>
                                <field name="created_by"/>
                                <field name="partner_id" options="{'no_create': True}"/>
                                <field name="total_consumption_amount"/>
                                <field name="is_it_sold"/>
                                <field name="weekly_route_id"/> <!-- test  -->
                                <field name="ref"/> <!-- test  -->

                            </group>
                        </group>
                        <notebook>
                            <page string="تفاصيل" style="text-align: right;">
                                <group invisible="is_it_sold" col="5">
                                    <label for="reason_ifNotSold" string="في حال الاجابة لا، اذكر السبب مع إرفاق صورة"
                                           class="text-muted"/>
                                    <separator string="" colspan="4"/>
                                    <group col="2">
                                        <field name="attachment_ifNotSold"
                                               string="ارفق الصورة"
                                               widget="binary"
                                               class="oe_inline"
                                        />

                                        <field name="reason_ifNotSold"
                                               string="السبب"
                                               placeholder="اذكر السبب..."
                                               widget="text"
                                               style="height: 120px; width: 300px;"/>
                                    </group>
                                </group>

                                <group invisible="not is_it_sold">
                                    <label for="order_id" string="في حال الإجابة نعم" class="text-muted"/>
                                    <separator string="" colspan="4"/>

                                    <group col="2">

                                        <field name="today_date" invisible="1"/>
                                        <group>
                                            <field name="attachment_before" widget="binary" class="oe_inline"/>
                                        </group>
                                        <separator string="" colspan="4"/>
                                        <group>
                                        <button name="%(new_act_res_partner_2_sale_order3)d"
                                                string="بيع"
                                                class="btn-primary"
                                                type="action"
                                                context="{'search_default_quotation': 1, 'default_type': 'quotation', 'default_partner_id': partner_id}"
                                                groups="salesman_manager.group_salesman"/>
                                         </group>
                                        <field name="order_id" widget="many2many_tags" groups="salesman_manager.group_salesman_supervisor"/>
                                        <group>
                                            <field name="attachment_after" widget="binary" class="oe_inline"/>
                                        </group>
                                    </group>
                                </group>
                            
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Search View for Daily Visits -->
        <record id="view_daily_visit_search" model="ir.ui.view">
            <field name="name">daily.visit.search</field>
            <field name="model">daily.visit</field>
            <field name="arch" type="xml">
                <search string="الزيارات">
                    <field name="reference"/>
                    <field name="partner_id"/>
                    <field name="created_by"/>
                </search>
            </field>
        </record>

        <!-- Action for supervisor: open tree view for daily visit-->
        <record id="action_daily_visit" model="ir.actions.act_window">
            <field name="name">الزيارات</field>
            <field name="res_model">daily.visit</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    قم بإنشاء أول زيارة
                </p>
            </field>
        </record>

        <!-- Action for Salesman: opens form view -->
        <record id="action_daily_visit_form_only" model="ir.actions.act_window">
            <field name="name">تسجيل زيارة</field>
            <field name="res_model">daily.visit</field>
            <field name="view_mode">form</field>
            <field name="target">current</field>
            <field name="context">{'form_view_initial_mode': 'edit'}</field>
        </record>

        <!-- Menu for Daily Visits - only for supervisors -->
        <menuitem id="menu_daily_visit"
                  name="الزيارات"
                  parent="menu_sales_man_root"
                  action="action_daily_visit"
                  sequence="30"
                  groups="salesman_manager.group_salesman_supervisor"/>
    </data>
</odoo>
