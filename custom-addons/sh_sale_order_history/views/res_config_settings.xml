<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.inherit.view.form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale.res_config_settings_view_form" />
        <field name="arch" type="xml">

            <xpath expr='//block[@name="catalog_setting_container"]' position="after">
                <block title="Sale Order History Configuration" name="sale_order_history_setting_container">
                    <setting >
                        <field name="sh_sale_configuration_limit" />
                    </setting>

                    <setting >
                        <field name="day" />
                    </setting>

                    <setting >
                        <field name="stages" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}" />
                    </setting>

                    <setting help="Enable Reorder Button For Sales order History">
                        <field name="enable_reorder" />
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
