<?xml version="1.0"?>
<odoo>
    <data>
        <!-- Inherit the pos.config form view -->
        <record id="pos_config_view_form_inherit_pos_cache" model="ir.ui.view">
            <field name="name">pos.config.form.inherit.pos.cache</field>
            <field name="model">pos.config</field>
            <field name="inherit_id" ref="point_of_sale.pos_config_view_form" />
            <field name="arch" type="xml">
                <!-- Find the 'other_devices' setting element and insert the new fields after it -->
               <xpath expr="//div[@class='row mt16 o_settings_container'][last()]" position="after">
                    <field name='oldest_cache_time'/>
                    <button name='delete_cache' type="object" string="Invalidate cache"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>