<odoo>
    <data>
        <!-- Cron Job to send unsent orders to ZATCA -->
        <!-- <record id="ir_cron_send_zatca_orders" model="ir.cron">
            <field name="name">Send Unsent Orders to ZATCA</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="state">code</field>
            <field name="code">model.send_pending_orders_to_zatca()</field>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=16, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="user_id" ref="base.user_root"/>
        </record> -->

        <!-- Another cron job at 2 AM -->
        <!-- <record id="ir_cron_send_zatca_orders_night" model="ir.cron">
            <field name="name">Send Unsent Orders to ZATCA at 2 AM</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="state">code</field>
            <field name="code">model.send_pending_orders_to_zatca()</field>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />            <field name="user_id" ref="base.user_root"/>
        </record> -->
    </data>
</odoo>
