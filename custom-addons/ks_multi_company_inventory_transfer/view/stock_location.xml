<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_location_form" model="ir.ui.view">
        <field name="name">stock.location.form</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='return_location']" position='after'>
                <!-- <field name="is_inter"  invisible="usage != 'internal'"/> -->
                <field name="is_from_inter"  invisible="usage != 'internal'"/>
                 <field name="is_inter_dolfin"  invisible="usage != 'internal'"/>
                
                <field name="transfer_to"  invisible="usage != 'internal'"/>
                 <field name="transfer_type"  invisible="usage != 'internal'"/>
                
              
            </xpath>
        </field>
    </record>
</odoo>